# Timestamp Microservice

This is a timestamp microservice project for freeCodeCamp, usage is outline below.

## Usage

Pass a string as a parameter in the url, and the app will check to see whether that string contains either a unix timestamp or a natural language date (example: January 1, 2016).